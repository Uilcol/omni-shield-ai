name: OmniShield AI Enterprise CI/CD

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  security-events: write

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # -----------------------------
      # Setup Rust
      # -----------------------------
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      # -----------------------------
      # Build Rust Workspace
      # -----------------------------
      - name: Build OmniShield AI
        run: cargo build --workspace --release

      # -----------------------------
      # Run OmniShield AI Scan + SARIF
      # -----------------------------
      - name: Run OmniShield AI Security Scan
        run: |
          cargo run -p cli --release -- scan . \
            --sarif omnishield.sarif \
            --fail-on high

      # -----------------------------
      # Upload SARIF to GitHub Security
      # -----------------------------
      - name: Upload SARIF Report
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: omnishield.sarif

      # -----------------------------
      # Setup Python
      # -----------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: pip install -r engine/requirements.txt

      # -----------------------------
      # Run Enterprise Pipeline
      # -----------------------------
      - name: Run Enterprise Full Stack Pipeline
        run: python engine/engine_pipeline_fullstack_enterprise.py

      # -----------------------------
      # Archive Reports
      # -----------------------------
      - name: Archive Audit Reports
        uses: actions/upload-artifact@v4
        with:
          name: omnishield-reports
          path: |
            reports/
            omnishield.sarif